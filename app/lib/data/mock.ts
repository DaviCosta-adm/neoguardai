/**
 * Compatibilidade: preferir `repository` + sessão autenticada.
 * Mantém apenas rótulos compartilháveis no client.
 */
export {
  rotuloIntervencao,
  rotuloStatusAcompanhamento,
} from "@/app/lib/data/labels";
